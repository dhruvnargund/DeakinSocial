doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Homepage
  // Google Fonts
  link(rel='preconnect' href='https://fonts.googleapis.com')
  link(rel='preconnect' href='https://fonts.gstatic.com' crossorigin='')
  link(href='https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700&display=swap' rel='stylesheet')
  // My CSS
  style 
    include ../public/css/app.css
  //- link(rel='stylesheet' href='../public/css/app.css')
.layout
  .layout-left
    h1.brand DeakinSocial
    .sidebar-menu
      .sidebar-menu-item
        a(href='http://localhost:3000/timeline') Homepage
      .sidebar-menu-item
        a(href='http://localhost:3000/') My Friends
        
      .sidebar-menu-item.sidebar-menu-item-active
        a(href='http://localhost:3000/profile/dnargund') My Profile
      .sidebar-menu-item
        a(href='http://localhost:3000/search') Search
  .layout-main
    .all-posts
      each r in result
        .post
          .post-main
            .post-header
              .post-username 
              a(href=`http://localhost:3000/profile/${r.username}`) #{r.username}
              .post-time #{r.time}
            .post-content #{r.content}
            each comment in r.comments 
              .post-username-comments
              a(href=`http://localhost:3000/profile/${post.username}`) #{comment.username}
              .comments #{comment.comment}

